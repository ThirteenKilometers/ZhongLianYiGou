<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mitime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 该应用测试页面 -->
        <activity android:name=".test.TestActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <!-- 该应用入口页面 -->
        <activity android:name=".modules.StartActivity">

        </activity>
        <!-- 该应用菜单主页面 -->
        <activity
            android:name=".modules.menu.MenuActivity"
            android:screenOrientation="portrait" />
 <!--       <activity android:name=".modules.menu.merchant.MerchantsClassificationActivity" />-->
        <!--商家2级分类-->
        <activity android:name=".modules.menu.merchant.merchant_two.MerchantTwoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>